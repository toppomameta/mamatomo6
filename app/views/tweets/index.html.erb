<div class="col-md-12">

  <!-- タブ・メニュー -->
  <ul class="nav nav-tabs">
  	<li><a href="#sampleContentA" data-toggle="tab">妊娠</a></li>
  	<li><a href="#sampleContentB" data-toggle="tab">仕事</a></li>
  	<li><a href="#sampleContentC" data-toggle="tab">料理</a></li>
  </ul>

  <!-- タブ内容 -->
  <div class="tab-content">
  	<div class="tab-pane" id="sampleContentA">
  		<p>タブＡの内容</p>
      <% @tweets.each do |tweet| %>
      <% if tweet.category == 1 then %>
        <div class="article">
          <p>投稿：<%= truncate(tweet.description, length: 50) %></p>
          <!-- <p>画像：<%=  %></p> -->
          <p>作成日：<%= tweet.created_at.strftime('%Y年%m月%d日 %H:%M:%S') %></p>
          <p>
            <%= image_tag tweet.image, size: "50x50" %>
            <%= tweet.user.nick_name %>
          </p>
          <p><%= link_to "続きを読む", tweet, class: "btn btn-default" %></p>
        </div>
        <% end %>
      <% end %>
  	</div>
  	<div class="tab-pane active" id="sampleContentB">
  		<p>タブＢの内容</p>
      <% @tweets.each do |tweet| %>
      <% if tweet.category == 2 then %>
        <div class="article">
          <p>投稿：<%= truncate(tweet.description, length: 50) %></p>
          <!-- <p>画像：<%=  %></p> -->
          <p>作成日：<%= tweet.created_at.strftime('%Y年%m月%d日 %H:%M:%S') %></p>
          <p>
            <%= image_tag tweet.image, size: "50x50" %>
            <%= tweet.user.nick_name %>
          </p>
          <p><%= link_to "続きを読む", tweet, class: "btn btn-default" %></p>
        </div>
        <% end %>
      <% end %>
  	</div>
  	<div class="tab-pane" id="sampleContentC">
  		<p>タブＣの内容</p>
      <% @tweets.each do |tweet| %>
      <% if tweet.category == 3 then %>
        <div class="article">
          <p>投稿：<%= truncate(tweet.description, length: 50) %></p>
          <!-- <p>画像：<%=  %></p> -->
          <p>作成日：<%= tweet.created_at.strftime('%Y年%m月%d日 %H:%M:%S') %></p>
          <p>
            <%= image_tag tweet.image, size: "50x50" %>
            <%= tweet.user.nick_name %>
          </p>
          <p><%= link_to "続きを読む", tweet, class: "btn btn-default" %></p>
        </div>
        <% end %>
      <% end %>
  	</div>
  </div>




<!--nav_area-->
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
  <ul class="nav navbar-nav navbar-left">
    <!-- ★★★★下記の#を後ほど正しいURLに変更 -->
    <li><a href="#">妊娠・子育て</a></li>
    <li><a href="#">仕事・キャリア</a></li>
    <li><a href="#">お料理・レシピ</a></li>
  </ul>
 </div>
 <!--//nav_area-->
  <h1>twitter記事一覧</h1>
  <% byebug %>
  <% @tweets.each do |tweet| %>
  <p>Category -> <%= tweet.category %></p>
    <div class="article">
      <p>投稿：<%= truncate(tweet.description, length: 50) %></p>
      <!-- <p>画像：<%=  %></p> -->
      <p>作成日：<%= tweet.created_at.strftime('%Y年%m月%d日 %H:%M:%S') %></p>
      <p>
        <%= image_tag tweet.image, size: "50x50" %>
        <%= tweet.user.nick_name %>
      </p>
      <p><%= link_to "続きを読む", tweet, class: "btn btn-default" %></p>
    </div>
  <% end %>
</div>
